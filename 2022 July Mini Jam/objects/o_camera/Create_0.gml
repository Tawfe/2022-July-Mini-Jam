cam = view_camera[0]
follow = o_player
view_w_half = camera_get_view_width(cam) * 0.5
view_h_half = camera_get_view_height(cam) * 0.5
xTo = xstart // or x (because its a create event)
yTo = ystart // or y (because its a create event)
